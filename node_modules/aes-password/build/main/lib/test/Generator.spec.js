"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const ava_1 = __importDefault(require("ava"));
const Generator_1 = require("../utils/Generator");
(0, ava_1.default)('IV is 16 bytes long', async (t) => {
    const iv = await Generator_1.Generator.generateIV();
    t.is(iv.length, 16);
});
(0, ava_1.default)('Salt generates specified length', async (t) => {
    const randomNumber = Math.round(Math.random() * 100);
    const nonNull = randomNumber === 0 ? 1 : randomNumber;
    console.log('Random number');
    const salt = await Generator_1.Generator.generateSalt(nonNull);
    t.deepEqual(salt.length, nonNull);
});
(0, ava_1.default)('Salt fails with wrong arguments', async (t) => {
    const res = await Generator_1.Generator.generateSalt(0).catch(() => {
        /* Its getting checked by outer class */
    });
    t.falsy(res);
});
